# Fullstack Demo (Backend + Frontend)

## Backend

```bash
cd backend
cp .env.example .env   # then edit DATABASE_URL and CLIENT_ORIGIN
npm install
npm start
```

## Frontend

```bash
cd frontend
cp .env.example .env   # edit VITE_API_URL if needed
npm install
npm run dev
```
